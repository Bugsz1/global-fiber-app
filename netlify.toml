[build]
  command = "pnpm install --config.minimum-release-age=0 && pnpm run build --config.minimum-release-age=0"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
